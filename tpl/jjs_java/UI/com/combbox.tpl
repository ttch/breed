<span class="span_frame">
<label  for="{{id}}"><font color="{{isred}}">{{name}}</font></label>
<select id="{{id}}" name="{{id}}">
			{% for item in options %}
				<option></option>
			{% endfor %}
</select>
</span>