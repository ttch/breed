<TableLayout
    android:id="@+id/{{id}}"
    android:layout_width="{{layout_width}}"
    android:layout_height="{{layout_height}}"
    android:stretchColumns="{{stretchColumns}}" >
    {{content}}
</TableLayout>
