   <TextView
   android:id="@+id/view_{{id}}"
   android:layout_width="{{layout_width}}"
   android:layout_height="{{layout_height}}"
   android:layout_weight="{{layout_weight}}"
   android:text="{{fieldchnname}}" />
<Spinner
android:id="@+id/{{id}}"
android:layout_width="{{layout_width}}"
android:layout_height="{{layout_height}}"
android:layout_weight="{{layout_weight}}"/>